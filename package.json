{
  "name": "domain-bookmarklet",
  "version": "1.0.0",
  "description": "Domain-aware bookmarklet system with state tracking and progress UI",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "prebuild": "mkdir -p build",
    "test": "npm run build && node server.js",
    "build:static": "npm run build && node scripts/prepare-static.js",
    "start": "node server.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "terser": "^5.36.0"
  }
}
